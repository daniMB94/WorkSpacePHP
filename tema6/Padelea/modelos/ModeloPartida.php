<?php


/*

$consulta = $conexion->prepare("INSERT INTO jugadores(fecha, hora, ciudad, lugar, cubierto, estado, idj1, idj2, dij3, idj4) VALUES (?,?,?,?,?,?,?,?,?,?)");
        $consulta->bindValue(1, $jugador->getFecha());
        $consulta->bindValue(2, $jugador->getHora());
        $consulta->bindValue(3, $jugador->getCiudad());
        $consulta->bindValue(4, $jugador->getLugar());
        $consulta->bindValue(5, $jugador->getCubierto());
        $consulta->bindValue(6, $jugador->getEstado());
        $consulta->bindValue(7, $jugador->getIdj1());
        $consulta->bindValue(8, $jugador->getIdj2());
        $consulta->bindValue(9, $jugador->getIdj3());
        $consulta->bindValue(10, $jugador->getIdj4());
        $consulta->bindValue(2, $jugador->getHora());
*/

?>